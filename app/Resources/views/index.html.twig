<!DOCTYPE html>

<html>
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		{% block css %}
		<!--<link rel= "stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" />-->
		<link rel="stylesheet" href="{{ asset('bundles/bootstrap/css/bootstrap.min.css') }}" type="text/css" />
		
		{% endblock %}
		<title> {% block title %} Carnet d'adresse {% endblock %}</title>
	</head>
	
	<body>
	<nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">ADRESSBOOK | ONLINE</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
		{% block formlog %}
     
		{% endblock %}
        </div>
      </div>
    </nav>
		
		
			<div id="header" class="jumbotron">
			<div class="container">
				<h2> Bienvenue sur votre carnet d’adresse en ligne  </h2>
				<p>Ce produit a été développé dans le simple but de conserver en ligne vos contacts afin que vous puissiez y accéder n’importe où sur n’importe quelle plateforme </p>
				<div>
					{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
						Bonjour {{app.user.username}}  |  
						<a href="{{ path('fos_user_security_logout') }}">
							Se déconnecter 
						</a>
					{% else %}
						Pas encore inscrit ? <a href="{{ path('fos_user_registration_register') }}"> | S'inscrire </a>
					{% endif %}
				</div>
			</div>
				
			</div>
			
			
			<div id="menu" class="col-lg-2">
				<h3> </h3>
				<ul class="nav nav-pills nav-stacked">
				<li><a href="{{path('appartoo_carnet_homepage')}}">Accueil</a></li>
				<li><a href="{{path('appartoo_carnet_add')}}">Ajouter un membre</a></li>
				
				</ul>
			</div>
			
			
			

			{% if app.request.hasPreviousSession %}
				{% for type, messages in app.session.flashbag.all() %}
					{% for message in messages %}
						<div class="flash-{{ type }}">
							{{ message }}
						</div>
					{% endfor %}
				{% endfor %}
			{% endif %}
			
			<div id ="content" class="col-lg-10">
			{% block content %} {% endblock %}
			</div>
			
		<div class="col-lg-12"><p><h1></h1><br /></p></div>
		<nav class="navbar navbar-default navbar-fixed-bottom">
			  <div class="container">
				<p class="text-center"><strong>ADANZOUNNON Maxwell &copy; 2015</strong> </p>
			  </div>
		</nav>
		
		 <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
		 <script src="{{ asset('bundles/bootstrap/css/bootstrap.min.js') }}"></script>
	</body>
</html>